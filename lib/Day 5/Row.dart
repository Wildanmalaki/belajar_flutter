import 'package:flutter/material.dart';

class RowDay5 extends StatelessWidget {
  const RowDay5({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
     children: [
      Icon(Icons.location_on),
      Text("Pinang Ranti on the fire"),
     ],
    );
  }
}